# Tsdk Drizzle ORM example

Install

```sh
pnpm install
```

Run Server

```sh
pnpm dev:server
```

Run Nextjs app:

```sh
pnpm dev:next-app
```

Run Expo(react native) app:

```sh
pnpm dev:expo-app
```
